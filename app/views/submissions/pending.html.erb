<% content_for :left do %>
  <%= render :partial => "experiments/admin_left_menu" %>  
<% end -%>

<% content_for :right do %>
  <%= render :partial => "experiments/right_side_list_menu" %>  
<% end -%>
<hr />

<table width="100%" class="SAXSDisplay" >

   <tr class="ListLine2">
	<td colspan="1"><B>ID</B></td>
	<td colspan="1" align="center">
		<B>Created</B>
        <span class="data_date"></span></td>
    <td align="center"><B>Last Updated</B>
        <span class="data_date"></span></td>
	<td colspan="1"><B>BioIsis ID</B></td>
  </tr>
	
<% @submissions.each do |deposit| %>
   <tr class="ListLine2">
	<td colspan="1">
	<%= link_to "#{deposit.id}", {:controller => :experiments, :action => :approve, :id => deposit.data_directory} %></td>
	<th colspan="1" align="center">
		<span class="data_date"><%= nice_date(deposit.created_at) %>
        </span></td>
    <td><span class="data_date"><%= nice_date(deposit.updated_at) %></span></td>
	<td colspan="1"><%= link_to deposit.bioisis_id, {:controller => :experiments, :action => :approve, :id => deposit.data_directory} %></td>
  </tr>
 
<% end %>
<tr><th colspan="4" class="specalt"><%= will_paginate @submissions %></th></tr>
</table>